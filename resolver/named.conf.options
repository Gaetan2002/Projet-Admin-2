options {
    directory "/var/cache/bind";

    recursion yes;
    allow-query { localhost; 192.168.0.0/20;  };
    allow-recursion { any; };
    listen-on{ any; };

    dnssec-validation auto;
    auth-nxdomain no;
};
